import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  logoWrapper: {
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 28,
  },
  centerLogo: {
    opacity: 0.95,
  },
});
