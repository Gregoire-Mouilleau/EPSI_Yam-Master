import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  logoWrapper: {
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 28,
  },
  centerLogo: {
    width: 811,
    height: 406,
    opacity: 0.95,
  },
});
